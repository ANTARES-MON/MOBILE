# Project Setup Guide

## 1. Environment Configuration
Copy `local.properties.template` to `local.properties` and add your API keys.

## 2. Required Keys
- Google Maps API Key
- Firebase Configuration (if used)

## 3. Security
- Never commit `local.properties` or any `.keystore` files
- Use template files for configuration
